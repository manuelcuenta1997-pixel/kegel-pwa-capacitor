{
  "name": "kegel-pwa-capacitor",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:web": "pnpm --filter @app/web dev"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
